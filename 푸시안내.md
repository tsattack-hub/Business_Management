# GitHub 업로드 방법

이 폴더는 이미 git 저장소로 초기화되어 있고, 첫 커밋까지 되어 있습니다.
원격 주소만 연결해서 밀어 넣으면 됩니다.

## 1. 확인

```bash
cd Business_Management        # 압축을 푼 폴더
git log --oneline             # 커밋 1개가 보이면 정상
git status                    # 아무것도 안 나오면 정상
```

## 2. 원격 연결 후 푸시

```bash
git remote add origin https://github.com/tsattack-hub/Business_Management.git
git push -u origin main
```

`Username` / `Password` 를 물으면 —
비밀번호 자리에 **Personal Access Token** 을 넣어야 합니다. 계정 비밀번호는 2021년부터 막혔습니다.

토큰 발급: GitHub → Settings → Developer settings → Personal access tokens
→ Tokens (classic) → Generate new token → `repo` 권한 체크

### 원격 저장소에 이미 내용이 있다면

```bash
git push -u origin main --force
```

새로 시작하는 저장소라면 위 명령이 안전합니다.
기존 내용을 살려야 하면 `--force` 대신 `git pull --rebase origin main` 을 먼저 하세요.

## 3. 커밋 작성자 바꾸기

지금 커밋은 `tsattack-hub <tsattack-hub@users.noreply.github.com>` 으로 되어 있습니다.
본인 정보로 바꾸려면 푸시 **전에**:

```bash
git config user.name "이름"
git config user.email "메일주소"
git commit --amend --reset-author --no-edit
```

---

## 커밋에서 빠진 것 (의도적)

`.gitignore` 로 제외했습니다.

| 경로 | 이유 |
|---|---|
| `projects/` | 사업 데이터. 실제 사업명·금액이 들어갑니다 |
| `생성서류/` | 만들어진 결과물. 저장소에 둘 이유가 없습니다 |
| `__pycache__/` | 파이썬 캐시 |
| `.streamlit/` | 로컬 설정 |

`projects/` 는 특히 주의하십시오. 실제 사업 정보가 공개 저장소에 올라갑니다.
